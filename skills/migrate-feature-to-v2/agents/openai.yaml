interface:
  display_name: "功能迁移到 AI 友好 2.0"
  short_description: "中断恢复强制 subagent 派工、前端细粒度拆分、任务清单跟踪和审批迁移能力"
  default_prompt: "Use $migrate-feature-to-v2 to initialize a project-local visual migration workspace, split migration work into one-pass-feasible task packages, force subagent assignment after resume/interruption, create a thin frontend surface index before frontend micro-packages, separate frontend/backend surfaces, reject copied legacy paths with a dross scan, wait for design approval, implement, and finish with a completion check."
